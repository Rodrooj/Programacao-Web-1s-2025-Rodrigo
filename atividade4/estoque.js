let item = []

function adicionar(id, nome, qtd){
    item.push(item)
}

function listar(){
    return item
}

function remover(id){
    item.pop(item)
}

function editar(id, qtd){
    
}

module.exports = {
    adicionar,
    listar,
    remover,
    editar
}